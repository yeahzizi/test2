깃 루틴

git bash에서 mkdir 이름 > cd 이름 > git init > touch 이름.txt(txt는 예시 파이썬은 py가 될것) 
> code . 을 하면 vs code 창이 뜸

git config > 코드를 치고 > add > commit >status > log 
> 깃허브 홈페이지에서 주소 따오고(처음에만) 
> git push origin master(이거는 처음에는 필요없음/ 두번째부터 계속 함)  
서클이 하나 끝**깃 루틴)**

git bash에서 mkdir 이름 > cd 이름 > git init > touch 이름.txt(txt는 예시 파이썬은 py가 될것) > code . 을 하면 vs code 창이 뜸

git config > 코드를 치고 > add > commit >status > log > 깃허브 홈페이지에서 주소 따오고(처음에만) > git push origin master(이거는 처음에는 필요없음/ 두번째부터 계속 함) 
서클이 하나 끝